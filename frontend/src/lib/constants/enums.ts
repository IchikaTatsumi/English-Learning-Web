export enum DifficultyLevel {
  BEGINNER = 'Beginner',
  INTERMEDIATE = 'Intermediate',
  ADVANCED = 'Advanced',
  MIXED = 'Mixed Levels',
}

export enum Role {
  ADMIN = 'Admin',
  USER = 'User',
}

export enum ViewModeEnum {
  LIST = 'List',
  GRID = 'Grid',
}
